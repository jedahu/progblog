box: debian
build:
  steps:
    - arjen/hugo-build@1.8.1:
        version: "0.15"
        theme: herring-cove
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - ysqi/git-push@0.7.15:
        gh_oauth: $GIT_TOKEN
        branch: master
        basedir: public
